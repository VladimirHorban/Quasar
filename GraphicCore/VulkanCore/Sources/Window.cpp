#include "../Headers/Window.h"

Window::Window(const char* aWindowName, int aWidth, int aHeight) : mWindowName(aWindowName), mWidth(aWidth), mHeight(aHeight)
{
		
}

Window::~Window()
{
	
}

void Window::initWindow()
{
	
}